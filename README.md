# NeRFwithTensorDecompositions
Tensor Decompositions in NeRF

Установка библиотек
```
pip install -r requirements.txt
```

Запуск
```
python3 -m src.pipeline.run_nerf --config src/configs/skeletonnf_lego.txt --device cuda
```